name: Docker Build and Publish (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow is disabled."
          echo "Use .github/workflows/ci.yml which gates pushes on tests + smoke."
